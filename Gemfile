#Gemfile

source 'https://rubygems.org'

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.5.1'
gem 'sinatra'
gem 'mongoid'
gem 'sinatra-contrib'

group :development do
  gem 'rerun'
  gem 'pry'
end
